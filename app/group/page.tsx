import Link from 'next/link'; import { groupCompanies } from '../../data/mock-data'; import { ArrowRight } from 'lucide-react';
export default function Group(){return <><section className="page-hero"><div className="container"><div className="eyebrow">Graycell / Group architecture</div><h1>ONE BRAND.<br/>MULTIPLE CAPABILITIES.</h1><p>Graycell is the master brand, with the underlying companies represented clearly without allowing corporate structure to dominate the customer journey.</p></div></section><section className="page-content"><div className="container"><div className="panel pad"><div className="eyebrow">MASTER BRAND</div><div style={{fontSize:'clamp(54px,9vw,120px)',fontWeight:700,letterSpacing:'-.06em',margin:'12px 0 42px'}}>GRAY<span style={{color:'var(--blue)'}}>CELL</span></div><div className="feature-grid">{groupCompanies.map((c,i)=><div className="panel pad" key={c.id}><div className="num">0{i+1}</div><h2 style={{fontSize:26,margin:'14px 0 10px'}}>{c.name}</h2><p>{c.summary}</p><div className="tag-row" style={{marginTop:15}}>{c.capabilities.map(x=><span className="tag" key={x}>{x}</span>)}</div></div>)}</div></div><div className="notice" style={{marginTop:22}}>The exact legal relationship and descriptions should be confirmed before production launch. Director names and personal/direct phone numbers are intentionally excluded.</div><Link className="btn btn-primary" style={{marginTop:24}} href="/contact">Contact Graycell <ArrowRight size={15}/></Link></div></section></>}
