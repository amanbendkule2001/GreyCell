import { notFound } from 'next/navigation'; import { projects } from '../../../data/mock-data';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function ProjectDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return notFound();return <><section className="page-hero"><div className="container"><div className="eyebrow">Projects / Application showcase</div><h1>{p.title}</h1><p>Source-backed application imagery from the supplied Graycell catalogue. This page is not presented as a verified case study.</p></div></section><section className="page-content"><div className="container"><div className="gallery"><img src="/images/projects/css-installation-03.jpg" alt="Compact substation installation"/><img src="/images/projects/css-installation-01.jpg" alt="Compact substation installation"/><img src="/images/projects/css-installation-02.jpg" alt="Compact substation installation"/></div><div className="notice" style={{marginTop:24}}>Verification status: application showcase. Project scope, client, capacity, location and outcome claims should only be added after client approval.</div></div></section></>}
