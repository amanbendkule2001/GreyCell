'use client'; import {useState} from 'react'; import Link from 'next/link'; import {ArrowRight,CheckCircle2} from 'lucide-react';
const steps=['Requirement type','Application','Capacity','Voltage','Installation','Special requirements','Contact'];
export default function Builder(){const [step,setStep]=useState(0);const [done,setDone]=useState(false);return <><section className="page-hero"><div className="container"><div className="eyebrow">Engineering intake / Guided selection</div><h1>BUILD YOUR<br/>REQUIREMENT.</h1><p>Define the project context step by step. This is a preliminary intake tool, not automatic engineering approval.</p></div></section><section className="page-content"><div className="container"><div style={{display:'grid',gridTemplateColumns:`repeat(${steps.length},1fr)`,gap:5,marginBottom:36}}>{steps.map((x,i)=><div key={x} style={{height:4,background:i<=step?'var(--blue)':'#dfe6ec'}} title={x}/>)}</div>{done?<div className="panel pad" style={{textAlign:'center',maxWidth:760,margin:'0 auto'}}><CheckCircle2 color="#16834b" size={48}/><h2>READY FOR ENGINEERING.</h2><p>The preliminary requirement summary is ready. Open the enquiry form to continue to the production workflow.</p><Link className="btn btn-primary" href="/contact">Open Enquiry Form</Link></div>:<div className="two-col"><div className="panel pad"><div className="eyebrow">STEP {step+1} / {steps.length}</div><h2>{steps[step]}</h2>{step===0?<div className="feature-grid" style={{gridTemplateColumns:'1fr 1fr'}}>{['Transformer','Compact Substation','MV System','Not Sure'].map(x=><button key={x} className="btn btn-outline" style={{height:78}} onClick={()=>setStep(1)}>{x}</button>)}</div>:<><div className="field"><label>{steps[step]}</label>{step===2?<input placeholder="e.g. 1000 kVA"/>:step===3?<input placeholder="e.g. 11 kV / 415 V"/>:step===4?<select><option>Indoor</option><option>Outdoor</option><option>Unknown</option></select>:step===5?<textarea rows={7} placeholder="Environmental, installation or specification requirements"/>:<input placeholder="Enter requirement"/>}</div><button className="btn btn-primary" style={{marginTop:20}} onClick={()=>step===steps.length-1?setDone(true):setStep(step+1)}>{step===steps.length-1?'Prepare Requirement':'Continue'} <ArrowRight size={15}/></button></>}</div><aside className="sticky"><div className="panel pad"><div className="eyebrow">REQUIREMENT SUMMARY</div>{steps.map((s,i)=><div key={s} style={{padding:'12px 0',borderBottom:'1px solid var(--line)',display:'flex',justifyContent:'space-between',fontSize:12}}><span>{s}</span><span className="num">{i<=step?'SET':'—'}</span></div>)}</div></aside></div>}</div></section></>}
