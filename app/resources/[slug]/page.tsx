import { notFound } from 'next/navigation'; import { resources } from '../../../data/mock-data'; import Link from 'next/link'; import { Download } from 'lucide-react';
export function generateStaticParams(){return resources.map(r=>({slug:r.slug}))}
export default async function ResourceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=resources.find(x=>x.slug===slug);if(!r)return notFound();return <><section className="page-hero"><div className="container"><div className="eyebrow">Resources / Catalogue</div><h1>{r.title}</h1><p>{r.seo.description}</p></div></section><section className="page-content"><div className="container two-col"><div className="panel"><img src="/images/brand/catalogue-page-1.jpg" alt="Graycell product catalogue" style={{width:'100%',height:560,objectFit:'cover',objectPosition:'top'}}/></div><div className="panel pad"><div className="eyebrow">SOURCE STATUS</div><h2>Supplied Graycell catalogue</h2><p>The PDF is included in this frontend build so visitors can access the source document directly.</p>{r.file&&<a className="btn btn-primary" href={r.file.url} target="_blank" rel="noreferrer"><Download size={16}/> Download PDF</a>}<div className="notice" style={{marginTop:22}}>Only source-backed product and capability claims should be promoted to production content without review.</div><Link className="text-link" style={{display:'inline-flex',marginTop:22}} href="/contact">Ask about a technical document <span>→</span></Link></div></div></section></>}
